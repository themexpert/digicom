var gulp = require('gulp');

var config = require('../../gulp-config.json');
var extPath = '.';

var baseTask  = 'modules.modules';
var extPath   = './src';
// var mediaPath = extPath + '/media';
//
// Clean
gulp.task('clean:' + baseTask,function() {
		return true;
});
